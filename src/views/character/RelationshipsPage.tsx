// src/views/character/RelationshipsPage.tsx
export default function RelationshipsPage({
  characterId,
}: {
  characterId: string;
}) {
  return <div>Relationships Page for character {characterId}</div>;
}
